# Deployment requirements — versions MUST match model training environment
# Model was trained with sklearn 1.8.0 / numpy 2.4.4 / scipy 1.17.1
# Using mismatched versions will cause model.joblib deserialization failures

# Core ML (must match training environment exactly)
scikit-learn==1.8.0
numpy==2.4.4
scipy==1.17.1
joblib==1.5.3
lightgbm==4.6.0
google-genai>=1.0.0

# API Framework
fastapi==0.115.0
uvicorn[standard]==0.32.1
slowapi==0.1.9
pydantic==2.10.3
python-multipart==0.0.18

# Auth & DB
bcrypt>=4.0.0
psycopg2-binary>=2.9.0
python-dotenv==1.0.0
requests>=2.31.0
google-auth>=2.20.0
spacy>=3.8.0
