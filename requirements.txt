# ============================================
# Production API dependencies (api.py on Render)
# ============================================

# Core ML
numpy>=2.0.0
scipy>=1.12.0
scikit-learn>=1.4.0
joblib>=1.3.0
lightgbm>=4.0.0

# API Framework
fastapi>=0.115.0
uvicorn>=0.30.0
slowapi>=0.1.9
pydantic>=2.0.0
python-multipart>=0.0.18

# Auth & Security
bcrypt>=4.0.0

# Utils
python-dotenv>=1.0.0
requests>=2.31.0

# NOTE: spacy is NOT required — utils.py has a built-in fallback
# that uses regex-based cleaning when spaCy is not installed.
# This avoids a 500MB+ install that frequently fails on Render.

# NOTE: streamlit, matplotlib, plotly, xgboost, qrcode,
# textblob, vaderSentiment, google-generativeai, etc.
# are NOT needed for the production API server.
# They are only used by the training script (train.py) and
# the deprecated Streamlit frontend (streamlit_app.py).
